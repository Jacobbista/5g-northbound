{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://jacobbista.github.io/5g-northbound/schema/device-diagnostics.schema.json",
  "title": "Device diagnostics (private-profile extension)",
  "description": "Response of GET /device-diagnostics/v0/{assetId}. A private-profile extension, not CAMARA Device Location.",
  "type": "object",
  "required": [
    "assetId",
    "source",
    "diagnostics"
  ],
  "properties": {
    "assetId": {
      "type": "string"
    },
    "source": {
      "type": "string"
    },
    "diagnostics": {
      "type": "object",
      "description": "Core vocabulary fields at the top; everything else under vendorSpecific. See docs/profile-extensions.md.",
      "properties": {
        "battery": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "LwM2M 3/0/9, percent"
        },
        "accuracy": {
          "type": "number",
          "description": "omlox accuracy, metres"
        },
        "moving": {
          "type": "boolean",
          "description": "derived from omlox speed"
        },
        "lastSeen": {
          "type": "number",
          "description": "omlox timestamp_generated, epoch seconds"
        },
        "vendorSpecific": {
          "type": "object",
          "description": "Values an integrator mapped that this profile does not define. Keys are chosen by whoever wrote the vendor schema, are not comparable across vendors, and are carried as authored. Non-authoritative.",
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    }
  }
}
