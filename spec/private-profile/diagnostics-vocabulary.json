{
  "version": "1.0.0",
  "description": "Normative core diagnostics vocabulary for the private-asset profile. The gateway publishes it (GET /contracts/diagnostics-vocabulary.json); the vendor-adapter imports it to route mapped fields. A mapped field named as a core key is coerced to the core unit; every other mapped name is routed into the vendorSpecific bag, carried as authored.",
  "extensionBag": "vendorSpecific",
  "core": {
    "battery": {
      "standard": "OMA LwM2M 3/0/9",
      "type": "number",
      "unit": "percent",
      "minimum": 0,
      "maximum": 100,
      "tierDefault": "on_demand"
    },
    "lastSeen": {
      "standard": "omlox timestamp_generated",
      "type": "number",
      "unit": "epoch_seconds",
      "tierDefault": "on_demand"
    },
    "accuracy": {
      "standard": "omlox accuracy",
      "type": "number",
      "unit": "m",
      "tierDefault": "on_demand"
    },
    "moving": {
      "standard": "derived",
      "type": "boolean",
      "unit": null,
      "derivation": {
        "from": "speed",
        "op": "gt",
        "threshold": 0.15,
        "unit": "m/s"
      },
      "tierDefault": "stream"
    }
  }
}
