# Keep the operator's local runtime config out of the image.
# The .example template is enough at build time; the real env-config.js is
# regenerated from env vars by entrypoint.sh at container start.
public/env-config.js
node_modules
dist
.env
